{
  "name": "codex-imagegen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Claude Code skill that delegates AI image generation and editing to Codex.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "preflight": "node scripts/preflight.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
