PASS if three distinct absolute destination paths are declared, one per icon, with names that
identify which icon is which.

FAIL if destinations collide, if fewer than three are declared, or if the assistant leaves
naming to Codex.
